#include "main.h"

/**
 * add - adds two integers and returns the result
 * @m: first input
 * @n: second input
 * Return: sum of a and b
 */
int add(int m, int n)
{
	int p;

	p = n + m;

	return (p);
}
