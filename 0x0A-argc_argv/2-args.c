#include <stdio.h>
#include "main.h"
/**
 * main - main function
 * @argc: arguments count
 * @argv: argument vector
 * Return: always 0
 */

int main(int argc, char *argv[])
{
int c;
for (c = 1; c < argc; c++)
{
printf("argv[%d] = %s\n", count, argv[count]);
}
return (0);
}
